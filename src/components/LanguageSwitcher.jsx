import { createElement } from 'react'; import { useLanguage } from '../context/LanguageContext'; export default function LanguageSwitcher({ className }) { const { lang, toggleLang } = useLanguage(); const label = lang === 'en' ? 'العربية' : 'English'; return createElement('button', { type: 'button', onClick: toggleLang, className: className || 'text-sm font-semibold px-3 py-1.5 rounded-full border border-white/20 text-white hover:bg-white/10 transition-colors' }, label); }
